import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';
import { Providers } from './providers';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'RightRoute - Your Pocket Guide to Knowing Your Rights',
  description: 'Instant, simplified legal information and scripts for law enforcement interactions',
  keywords: 'legal rights, law enforcement, civil rights, legal scripts, mobile app',
  authors: [{ name: 'RightRoute Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  );
}
